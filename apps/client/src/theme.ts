import { createTheme, Theme } from '@mui/material';

export const themeUGram: Theme  = createTheme({
  palette: {
    primary: {
      main: '#ffc103',
    },
    secondary: {
      main: '#e30513',
    },
  },
});
